{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/auth-service/Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"yarn prisma:migrate:deploy --schema=/app/src/prisma-setup/schema.prisma && yarn prisma db seed --schema=/app/src/prisma-setup/schema.prisma && node dist/main.js\"",
    "healthcheckPath": "/api/health-check",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
